{
  "name": "application-name",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "start": "node app.js",
    "test": "mocha --reporter spec --ui tdd --bail"
  },
  "dependencies": {
    "express": "3.5.1"
  }
}
